# Mobile App

Simple React Native app to view materials and send requests to the warehouse server.

This is only a skeleton. To run it, install React Native CLI and configure Android/iOS environments.

```bash
npm install
npm start
```
